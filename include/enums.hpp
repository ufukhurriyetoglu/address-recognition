#pragma once

namespace address_recognition {
    namespace enums {
        enum class TOKEN_TYPE {
            NONE,
            ZIP,
            COMMA,
            CAPITAL_AT_BEG,
            NUMBER
        };
    }
}